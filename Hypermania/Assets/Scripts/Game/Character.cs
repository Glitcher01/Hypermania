using System;

namespace Game
{
    public enum CharacterAnimation
    {
        Walk,
        Jump,
        Idle,
        LightAttack,
    }

    [Serializable]
    public enum Character
    {
        SampleFighter,
    }
}
